{% extends "layout.html.twig" %}
{% block title %} Accueil Jury {% endblock %}



{% block navigation %}
<button type="button" class="btn btn-default btn-block"  > <span class="glyphicon glyphicon-user">  </span> 					
{{ jure.prenomJure|title }} {{ jure.nomJure|title }} 
  </button>
  <br />
  
    {{ include ("cyberjury/navigation.html.twig") }}
{% endblock %}


{% block body %}
{% for equipes in listEquipes %}
    <p>
    <a
    {% if (progression[equipes.lettre]) %}  class ="btn btn-primary btn-lg" {% else %} class ="btn btn-default btn-lg" {% endif %}
href="{{ path('cyberjury_evaluer_une_equipe', {'id': equipes.id} ) }}" >{{ equipes.lettre }}</a> 

    {{ equipes.titreProjet }}
    <a href="{{ equipes.urlMemoire }}" class="glyphicon glyphicon-book" title="Accéder au mémoire de l'équipe" target="_blank"> </a>
    <a href="{{ path('cyberjury_infos_equipe', {'id': equipes.id} ) }}" class="glyphicon glyphicon-info-sign" title="En savoir plus sur cette équipe"> </a>
    <a href="{{ path('cyberjury_phrases_amusantes', {'id': equipes.id} ) }}" class="glyphicon glyphicon-pencil" title="Les phrases amusantes" > </a>
    <p />

{% endfor %}
{% endblock %}